---
phase: 04-test-infrastructure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/data/blogLoader.test.ts
autonomous: true
requirements:
  - QA-01
  - QA-02

must_haves:
  truths:
    - "Running npm run test executes a Vitest runner"
    - "Loader parser helpers and fallback mechanisms are covered by test assertions"
  artifacts:
    - path: "package.json"
      provides: "project scripts"
      contains: "vitest"
    - path: "src/data/blogLoader.test.ts"
      provides: "unit tests for loader"
      contains: "describe"
---

<objective>
Configure Vitest runner and add unit test coverage for the CMS/fallback loaders to prevent blog ingestion regressions.

Purpose: Guarantee data schema stability and regression-free development on subsequent portfolio site adjustments.
Output: Added dependency and script configuration, and functional unit tests at `src/data/blogLoader.test.ts`.
</objective>

<execution_context>
@.planning/phases/04-test-infrastructure/04-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@src/data/blogLoader.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and add scripts</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>Install `vitest` as a dev dependency and add a `"test": "vitest run"` script to the scripts section of `package.json`.</action>
  <verify>Check package.json scripts and devDependencies</verify>
  <acceptance_criteria>
    - devDependencies contains vitest
    - scripts.test is configured to "vitest run"
  </acceptance_criteria>
  <done>Vitest dependency and script added</done>
</task>

<task type="auto">
  <name>Task 2: Implement loader unit test suite</name>
  <files>src/data/blogLoader.test.ts</files>
  <read_first>src/data/blogLoader.ts</read_first>
  <action>Create unit test suite `src/data/blogLoader.test.ts`. Mock global `fetch` API responses to test slug sanitization, CMS parsing logic, and fallback behaviors.</action>
  <verify>Check for assertions on slugifying, validation, and local fallback parsing</verify>
  <acceptance_criteria>
    - src/data/blogLoader.test.ts contains assertions for loader logic
  </acceptance_criteria>
  <done>Loader unit test suite implemented</done>
</task>

<task type="auto">
  <name>Task 3: Execute tests and verify success</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>Run the tests using `npm run test` and check that the runner completes with all assertions passing.</action>
  <verify>npm run test</verify>
  <acceptance_criteria>
    - npm run test exits with code 0
  </acceptance_criteria>
  <done>Tests executed and passing</done>
</task>

</tasks>

<verification>
- [ ] npm run test runs and succeeds
- [ ] test file contains assertions on loaders fallbacks
</verification>

<success_criteria>
- Clean runner run.
- Main loader modules are fully tested.
</success_criteria>

<output>
After completion, create `.planning/phases/04-test-infrastructure/04-01-SUMMARY.md`
</output>
