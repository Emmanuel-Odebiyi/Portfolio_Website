---
phase: 02-config-consolidation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/blogLoader.ts
  - src/data/sanityClient.ts
autonomous: true
requirements:
  - CMS-03

must_haves:
  truths:
    - "A unified sanityClient module exports configuration and fetch functionality"
    - "blogLoader uses sanityClient instead of inline API calls"
  artifacts:
    - path: "src/data/sanityClient.ts"
      provides: "centralized sanity integration"
      contains: "SANITY_PROJECT_ID = '96ilx2qv'"
---

<objective>
Consolidate Sanity CMS configuration and API queries into a unified `sanityClient.ts` module, and refactor `blogLoader.ts` to utilize it.

Purpose: Eliminate hardcoded duplication of Sanity CMS configurations and ensure unified edge caching behavior across all queries.
Output: Centralized `src/data/sanityClient.ts` client module and updated `src/data/blogLoader.ts` loader.
</objective>

<execution_context>
@.planning/phases/02-config-consolidation/02-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/data/blogLoader.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified Sanity client module</name>
  <files>src/data/sanityClient.ts</files>
  <read_first>src/data/blogLoader.ts</read_first>
  <action>Create a new file `src/data/sanityClient.ts` that consolidates project metadata and fetches data with configured request caching flags.</action>
  <verify>Check that the file exports SANITY_PROJECT_ID, SANITY_DATASET, and fetchSanityQuery</verify>
  <acceptance_criteria>
    - src/data/sanityClient.ts exists
    - It exports SANITY_PROJECT_ID, SANITY_DATASET, and fetchSanityQuery
  </acceptance_criteria>
  <done>Sanity client module created and verified</done>
</task>

<task type="auto">
  <name>Task 2: Refactor blogLoader to consume unified client</name>
  <files>src/data/blogLoader.ts</files>
  <read_first>src/data/blogLoader.ts</read_first>
  <action>Modify `src/data/blogLoader.ts` to import `fetchSanityQuery` and constants from `sanityClient.ts`, and replace the inline fetch implementation.</action>
  <verify>git diff src/data/blogLoader.ts</verify>
  <acceptance_criteria>
    - blogLoader.ts does not contain hardcoded "96ilx2qv"
    - blogLoader.ts uses fetchSanityQuery
  </acceptance_criteria>
  <done>blogLoader.ts refactored successfully</done>
</task>

<task type="auto">
  <name>Task 3: Verify execution using validation script</name>
  <files>scratch/validate_markdown.js</files>
  <read_first>scratch/validate_markdown.js</read_first>
  <action>Run the verification script `scratch/validate_markdown.js` to ensure the project remains functional and fallback behavior is preserved.</action>
  <verify>node scratch/validate_markdown.js</verify>
  <acceptance_criteria>
    - node scratch/validate_markdown.js exits with status 0
  </acceptance_criteria>
  <done>Execution verified successfully</done>
</task>

</tasks>

<verification>
- [ ] src/data/sanityClient.ts exists and compiles without errors
- [ ] node scratch/validate_markdown.js exits with status 0
- [ ] No hardcoded project ID remains in blogLoader.ts
</verification>

<success_criteria>
- Single location configuration definition.
- Build compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-config-consolidation/02-01-SUMMARY.md`
</output>
