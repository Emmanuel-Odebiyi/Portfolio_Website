---
phase: 03-mobile-viewport-tuning
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/globe.tsx
  - src/components/Aurora.tsx
autonomous: true
requirements:
  - MOB-01
  - MOB-02

must_haves:
  truths:
    - "The Globe component uses a static fallback for viewports under 768px"
    - "The Aurora canvas does not render on viewports under 768px, rendering a CSS fallback instead"
  artifacts:
    - path: "src/components/ui/globe.tsx"
      provides: "responsive WebGL globe"
      contains: "isMobile"
    - path: "src/components/Aurora.tsx"
      provides: "throttled/paused background animation"
      contains: "radial-gradient"
---

<objective>
Optimize performance on mobile viewports by disabling active WebGL scenes (Globe and Aurora) for viewport widths under 768px, substituting them with high-fidelity, hardware-accelerated static CSS fallbacks.

Purpose: Prevent frame rate stuttering and conserve system resources on mobile devices by removing heavy 3D canvases from the mobile render path.
Output: Updated, responsive `src/components/ui/globe.tsx` and `src/components/Aurora.tsx` components.
</objective>

<execution_context>
@.planning/phases/03-mobile-viewport-tuning/03-CONTEXT.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/globe.tsx
@src/components/Aurora.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize Globe component for mobile viewports</name>
  <files>src/components/ui/globe.tsx</files>
  <read_first>src/components/ui/globe.tsx</read_first>
  <action>Add a mobile viewport detection hook to `src/components/ui/globe.tsx`. If width < 768px, render a static image/SVG representation of the active map theme instead of launching the heavy Three.js Canvas.</action>
  <verify>Check for the presence of media query check and conditional rendering in src/components/ui/globe.tsx</verify>
  <acceptance_criteria>
    - Globe component handles isMobile state
    - WebGL Canvas is bypassed when isMobile is true
  </acceptance_criteria>
  <done>Globe component optimized for mobile viewports</done>
</task>

<task type="auto">
  <name>Task 2: Pause Aurora WebGL loop on mobile viewports</name>
  <files>src/components/Aurora.tsx</files>
  <read_first>src/components/Aurora.tsx</read_first>
  <action>Add a mobile viewport detection hook to `src/components/Aurora.tsx`. If width < 768px, bypass the Three.js Canvas rendering entirely and return a performant, static CSS radial gradient background.</action>
  <verify>Check for isMobile check and radial-gradient styling in src/components/Aurora.tsx</verify>
  <acceptance_criteria>
    - Aurora component bypasses Canvas when isMobile is true
    - Performant CSS fallback is returned on mobile
  </acceptance_criteria>
  <done>Aurora render loop paused on mobile</done>
</task>

<task type="auto">
  <name>Task 3: Verify execution using validation script</name>
  <files>scratch/validate_markdown.js</files>
  <read_first>scratch/validate_markdown.js</read_first>
  <action>Run the verification script `scratch/validate_markdown.js` to ensure the project remains functional and fallback behavior is preserved.</action>
  <verify>node scratch/validate_markdown.js</verify>
  <acceptance_criteria>
    - node scratch/validate_markdown.js exits with status 0
  </acceptance_criteria>
  <done>Execution verified successfully</done>
</task>

</tasks>

<verification>
- [ ] src/components/ui/globe.tsx compiles and implements isMobile check
- [ ] src/components/Aurora.tsx compiles and implements isMobile check
- [ ] node scratch/validate_markdown.js exits with status 0
</verification>

<success_criteria>
- Mobile users do not load WebGL Canvas elements.
- CSS static gradient matches the theme color schema.
</success_criteria>

<output>
After completion, create `.planning/phases/03-mobile-viewport-tuning/03-01-SUMMARY.md`
</output>
